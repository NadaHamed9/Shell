# Define a command to print the command history
define print_command
    set $i = 0
    while $i < argIndex
        printf  "%s\n " ,args[$i++] 
  
    end
end

document print_command
This command shall print the last command enterred to the shell.
ends
